import { InvalidInputAlertDirective } from './invalid-input-alert.directive';

describe('InvalidInputAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidInputAlertDirective();
    expect(directive).toBeTruthy();
  });
});
